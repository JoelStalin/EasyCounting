# Integration Assets

Este directorio contiene integraciones externas y vendor trees que no forman parte directa del backend FastAPI principal.

Contenido actual:

- `odoo/neo_do_localization`: snapshot bruto del arbol Odoo traido desde el ambiente `jabiya`.
- `odoo/getupsoft_do_localization`: variante refactorizada localmente, con branding GetUpSoft y mapeo adaptado a `dgii_encf`.
